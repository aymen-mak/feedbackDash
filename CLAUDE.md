# Claude Code Instructions

## Git Workflow

- After every commit, **always push immediately** to the active development branch without waiting to be asked.
- Use `git push -u origin <branch-name>` each time.
- The active branch follows the pattern `claude/<description>-<sessionId>`.
